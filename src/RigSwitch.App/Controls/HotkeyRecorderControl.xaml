<UserControl x:Class="RigSwitch.App.Controls.HotkeyRecorderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Focusable="True"
             d:DesignHeight="32"
             d:DesignWidth="250">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Border x:Name="DisplayBorder"
                Grid.Column="0"
                Background="#252526"
                BorderBrush="#3E3E42"
                BorderThickness="1"
                CornerRadius="4"
                Padding="8,5"
                Cursor="Hand"
                MouseDown="OnRecordBorderMouseDown">
            <TextBlock x:Name="DisplayTextBlock"
                       Text="Click to Record"
                       Foreground="#8E8E93"
                       FontSize="13"
                       VerticalAlignment="Center" />
        </Border>

        <Button x:Name="ClearButton"
                Grid.Column="1"
                Content="✕"
                Click="OnClearButtonClick"
                Padding="8,4"
                Margin="4,0,0,0"
                Background="#2D2D30"
                Foreground="#8E8E93"
                BorderBrush="#3E3E42"
                BorderThickness="1"
                Cursor="Hand"
                ToolTip="Clear hotkey"
                Focusable="False" />
    </Grid>
</UserControl>
